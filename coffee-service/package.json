{
  "name": "coffee-service",
  "version": "0.1.0",
  "description": "Frontend estático del servicio de cafés (solo UI).",
  "scripts": {
    "serve": "npx serve public -l 3000",
    "serve:8080": "npx serve public -l 8080",
    "start:server": "node server.js",
    "dev:server": "nodemon server.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16"
  }
}
